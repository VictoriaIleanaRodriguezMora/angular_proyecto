import { StudentsDirective } from './students.directive';

describe('StudentsDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentsDirective();
    expect(directive).toBeTruthy();
  });
});
